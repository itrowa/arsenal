(define (sumsq-of-bigger x y z)
  if (< x y)
    ;if成立的分支，现在再比较x和z即可
    (if (< x z) 
              ;zy
              ;xy
              )
    ;if不成立的分支，现在再比较y和z即可
    (if (< y z)
              ;xz
              ;xy
              )
    )
